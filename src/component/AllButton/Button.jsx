
import styles from './Button.module.css'

function Button() {
    return (
        <div>
            <button type="button" className={styles.title}>Click</button>
        </div>

    );
}

export default Button;